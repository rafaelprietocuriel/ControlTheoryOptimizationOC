function folder=getocmatfolder(docObj,foldertype)
% returns a folder within the ocmat folder structure
%
% folder=GETOCMATFOLDER()
folder=getocmatfolder(foldertype,modeltype(docObj),modelname(docObj));