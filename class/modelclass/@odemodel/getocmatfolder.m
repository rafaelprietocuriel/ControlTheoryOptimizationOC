function folder=getocmatfolder(odeObj,foldertype)
% returns a folder within the ocmat folder structure
%
% folder=GETOCMATFOLDER()
folder=getocmatfolder(foldertype,modeltype(odeObj),modelname(odeObj));